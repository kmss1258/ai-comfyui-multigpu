docker exec -u root -it comfyui-gpu0 bash -lc 'set -e; VENV_PY=/comfy/mnt/venv/bin/python; "$VENV_PY" -m pip install -U uv; "$VENV_PY" -m uv pip install -r /basedir/custom_nodes/qwen3-tts-comfyui/requirements.txt; "$VENV_PY" -m uv pip install "transformers==4.57.3" "qwen-tts==0.0.5"; "$VENV_PY" -c "import importlib.metadata as m; print(\"transformers=\", m.version(\"transformers\")); print(\"qwen-tts=\", m.version(\"qwen-tts\"))"'
